export const text = "Welcome to <package_name> Package. This is a old version of the package upgrade to new version to use it.";

export const DS = () => {
    console.log(text);

    //return text;
}
